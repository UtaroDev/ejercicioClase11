const array=[1,2,3,4,5];

const Callback1 = (num) => { 
    if (num>2) {
        return ("este numero es mayor a 2", $num);
    }
 };

console.log (Callback1(array));




//const encontrarNum = array.find(Callback1);
